---
apiVersion: v1
kind: Pod
metadata:
  name: pod2
spec:
  containers:

    - name: cont1
      image: nginx
      ports:
        - containerPort: 80
      envFrom:
        - configMapRef:
            name: cm1
        - configMapRef:
            name: cm2
        - secretRef:
            name: mysec2
